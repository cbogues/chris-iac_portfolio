# Copy to .env (gitignored) and replace with a real random value.
# Generate one with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SCIM_BEARER_TOKEN=changeme-generate-a-random-token
